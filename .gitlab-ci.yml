stages:
  - build
  - test

variables:
  SBT_OPTS: "-Xms512M -Xmx2G"

image: sbtscala/scala-sbt:latest

build:
  stage: build
  script:
    - sbt clean compile

test:
  stage: test
  script:
    - sbt test